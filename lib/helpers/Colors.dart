import 'package:flutter/material.dart';

const MaterialColor primaryColors = MaterialColor(
  0xFF30475E,
  <int, Color>{
    50: primaryColor,
    100: primaryColor,
    200: primaryColor,
    300: primaryColor,
    400: primaryColor,
    500: primaryColor,
    600: primaryColor,
    700: primaryColor,
    800: primaryColor,
    900: primaryColor,
  },
);

const Color primaryColor = Colors.green;
const Color backgroundColorLightTheme = Colors.white;
const Color backgroundColorDarkTheme = Colors.white;
const Color accentColor = Colors.green;
const Color splashBackColor = Colors.white;
const Color whiteColor = Colors.green;
const Color blackColor = Colors.green;
